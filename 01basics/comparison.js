console.log(null>0)
// === strict check also checks datatype 
// == "2"==2 returns true;
console.log("2"===2);
// IN this line no 4 we have see the there is a strict checking where they are not doing any conversion. 

console.log("2"==2)

// however,if we see in the line no 7 we will get output true and the reason is the conversion. 
// so we have keep these things in mind while doing javascript things.